Observación importante

Durante el desarrollo de la tarea se notó que la versión del compilador influía en el comportamiento de ciertas funciones. En particular, en la llamada a sistema write, el stack no estaba construido como se esperaba, puesto que los valores a recuperar no se encontraban en el tope de la pila, sino que en una posición superior, la cual depende de la versión del compilador que se esté usando. Por esta razón es que se adjuntan dos versiones del archivo syscall.c, la que viene por defecto dentro de la estructura de carpetas corresponde a una versión probada con gcc 4.6.1, mientras que el archivo syscall.c provisto en la raíz de la entrega, corresponde a una versión que funciona con gcc 4.4.4.
